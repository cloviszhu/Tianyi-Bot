# 交付审查与验证范围

## 非VM方向评估与下载清理

用户最新要求优先同机运行。明确删除5个本次下载/解压的大文件，8,898,465,106字节，删除后目标不存在，未进入回收站，可重新官方下载；不碰图库/代码/运行环境，未卸载VirtualBox或网卡。旧下载成功证据为历史。

源码确认窗口选择不能止于WeChat(hwnd)：新开群窗分支按名称全局FindWindow、Menu取全局首个、WeChatImage预览查找未传pid，存在跨账号同名窗口风险。现有群名/type检查不证明所属账号。记录全链路PID/HWND/归属/唯一性约束及无歧义才运行的改造计划；未实施或声称通过。主机真实运行保护保持，未执行真实微信探测/监听/收发。共享桌面的焦点和剪贴板竞争不能以绑定窗口消除。项目保持开放。

## 已授权的官方镜像下载

用户授权下载微软官方镜像，许可仍由用户确认。实际取得Windows11 25H2简体中文x64多版本v2 ISO，8,543,608,832字节；实际SHA256为7408581E67BC455EBAAFB9230E531ABF45B1C8864A22114A1B03893F897102E4，与微软页面对应语言值一致。只下载并校验，未挂载/启动setup，未安装guest或激活。证据范围仅为官方介质完整性，不证明许可、guest启动、微信兼容性。项目继续保持开放。

## 已授权的宿主系统部署

用户“准”对应上轮安装VirtualBox及隔离网络的明确问题，不作为最终验收。实际安装Oracle签名/官方SHA256一致的VirtualBox7.2.16-174877，仅主程序和host-only网卡，MSI退出0且未重启。VBoxSup/VBoxNetAdp运行；主机新网卡192.168.56.1/24，DHCP禁用；无桥接、USB驱动或扩展包。没有VM、guest OS或真实账号操作。系统证据仅覆盖宿主软件和网络安装，不覆盖虚拟机启动、guest防火墙、UI自动化或输入隔离。具体目标标识和回退见integration.md开头。本次保持项目开放。

## 0.2 VM/GUI扩展（本地候选，不关闭归档）

新增真实Tk/ttk桌面、guest独立控制器、TLS证书指纹＋密钥＋源IP限制、配置与绑定版本确认、后台线程与UI结果队列、图库缩略图。原核心复用。真实后端只允许VirtualBox guest；旧直接run入口已停用，防止绕过人工小号确认。

本轮70项测试通过，无跳过：包含4项真实Tk控件＋实际本机TLS的模拟后端交互，配置/绑定/启停、GUI关闭后重连、图片PhotoImage预览、服务断开。另测错误密钥/证书/来源、路径逃逸、桌面/窗口失效、关闭后禁止新启动及长暂停拒绝。所有账号/微信/guest桌面在这些测试中均为模拟，不是实机结果。

实际渲染检查了管理页和图库页，发现并修复底部提示裁切，增加滚动区；同时修复注入模拟客户端时地址未显示。截图保存在系统临时目录tianyi-gui-validation（非真实账号）。修复服务关闭竞态，保持停止超时的数据锁，避免第二实例抢占。主机日常运行环境单独安装于用户`.tianyi-bot/host-runtime`，不含微信后端。项目源码重新打包到验证环境0.2.0后执行回归；下方46项是前一候选的历史结果。

停止是尽力而为，已进入的原生UI调用不能保证立即中断。周期/发送前检查发现桌面失效、窗口改变或超过30秒运行检查中断会中止；短暂停顿不保证检出。同一进程内换号无法可靠识别，人工备注不伪装账号ID。运行时不允许换号，服务重启不保留绑定。

手册已给出机器实测条件、资源预算、VirtualBox/网络/防火墙安装影响和恢复、日常GUI操作、锁屏/休眠/RDP区分、可信镜像和版本取得限制。没有安装hypervisor、调整系统安全/自动登录、下载OS镜像、改变主机微信或操作真实账号。真实VM部署、4.0.5安装/登录、4.1.13.12兼容性、分离控制台后渲染、双账号输入隔离和真实群收发仍全部待授权验收。保留项目开放，不进行最终用户验收或归档。

安全边界：管理API限host-only静态IP仍需部署时落实防火墙；本机TLS测试不能证明VM网卡规则已生效。密钥依赖用户文件权限，无DPAPI/多用户强隔离/一键证书轮换；不暴露任意路径、命令或公网服务。恢复按集成手册停服整目录备份，在新目录恢复并必要时重新配对。

## 0.1历史候选

执行者：Codex 自审；没有声称独立第三方审查。日期：2026-09-14（纽约）。

## 已审查并修正

- 相同用户/群才可消费待加图；消息与图片分开处理；并发重复回调通过应用锁与事件ID过滤。
- 下载仅对有效待加图触发，下载失败保留操作至超时；相同内容图片按图库去重。
- 图片格式由内容决定，临时文件刷盘后原子替换，SQLite参数化提交；数据库失败不留下破损引用。数据库失败可能产生安全的无引用图片，重试可复用。
- 配置不执行脚本；关键词不参与路径；元数据路径解析不能逃出图片根目录。2500万像素和字节限制，验证后解码首帧，拒绝损坏/超大图片。
- 上一张仅在发送成功后记录，重启后保留；不存在图片和发送失败有友好反馈。
- 免费后端使用真实源码确认的API；没有调用不存在的ImageMessage.download。使用独立群窗口，并在发送前重查群名/类型。
- 禁用上游更新目录删除、文件日志和发送者OCR兜底；单个回调工作线程维持顺序；无法确认身份的消息忽略。图片临时副本在处理后清理，源文件从不删除。
- 真实运行要求显式run --live；默认demo。开发过程未构造WeChat对象，未点击/收发真实微信。
- 进程锁阻止同目录双实例；停止后整目录备份/恢复在测试中实际演练。

## 证据分类

最终候选验证结果：46/46测试通过（无跳过）；离线A/B演示通过；免费后端Git来源检查通过；pip依赖检查通过；安装后的Python源码与当前项目逐文件一致。独立验证环境位于系统临时目录，Python3.12.14、Pillow12.3.0、wxauto4 40.1.1。

1. 自动化/模拟：unittest覆盖核心、适配器回调/预览复制合同、配置、进程锁、重启和恢复。
2. 真实本地安装：新建独立虚拟环境，安装项目和固定免费Git后端；读取安装元数据和源码签名，比较安装代码与项目源码；不导入UI。
3. 演示：A发加图命令，B发图，A发图，B取图；仅A图入库。
4. 运行时证据：绑定最终revision的run-evidence记录保存在.oracle-workflow中。安装构建本身发生在最终捕获前，后续用依赖检查、安装源码一致性与演示验证最终内容。

## 未覆盖和剩余风险

真实群收发、4.1.13.12客户端UI兼容性、隐藏昵称的连续图片和长时间监听均未测试。固定免费上游仅声明4.0.5。不能将当前候选称为已完成真实微信端到端验收。

显示名可能重名、群改名会改变命名空间；UI重建和进程重启后的消息去重为尽力而为。发送和记录上次发送之间存在崩溃窗口。文件落盘与数据库不是一个跨介质原子事务，但先文件后元数据保留安全恢复路径。

交付候选可供离线验收；最终用户验收保持待定。用户实际接受后，按harness-spec专用决定与关闭步骤生成最终归档。后续真实测试需要用户明确指定并授权测试群。
# 0.3本机识别阶段自审与恢复

新增独立结构扫描/绑定服务及真实Tk窗口，默认管理器切换到本机识别；没有复用存在全局窗口查找风险的wxauto流程。无聊天读取、收发、clipboard或自动焦点动作；只识别白名单进程下明确UIA主窗。8秒子进程超时、后台单工作线程避免UIA阻塞界面。

验证结果：86/86无跳过（安装包与源码一致、原70项加16项）；新用例覆盖选择第二窗口、必须确认、代次过期、PID/HWND/进程创建时间/路径变化、同PID多主窗、owner/最小化/关闭、扫描错误、长暂停、前台未匹配、Tk确认清除及pythonw启动时使用隐藏python子进程返回JSON。截图人工检查默认界面文字和布局。真实Windows只读扫描成功发现1个候选，未绑定未收发，不能作为多账号真实验证。

剩余边界：PID/HWND不是账号ID；同窗口换号无法可靠识别，明确要求人工解绑。轮询不保证捕捉短暂锁屏。主机真实后端继续关闭，免费库的菜单/预览/群子窗隔离尚未实现；这不阻断当前只读识别交付，但阻断主机实际机器人启用。未来真实操作仍需明确账号/测试群授权。

恢复方案：关闭GUI结束本阶段，只读绑定在内存且不改图库。旧远程界面保留--remote，原数据格式、核心和启动主机真实后端的禁止规则不变。未关闭或归档harness change，不把“开始”登记为最终验收。
# 0.4审查：后台硬约束

安装包完整回归107/107通过，无跳过；新操作界面截图已人工检查，文本/控件可见。测试结果只支持本地及模拟范围。

当前真实后台加图/取图未实现，不能宣称已交付全流程机器人。已交付的本地开发为窗口归属纯策略、真实Tk本机配置/图库/离线演示及只读结构检查；真实启动在新旧管理入口均禁用，未安装/启动真实微信后端，未执行账号/聊天操作。

新增21项测试覆盖所有者链、同名不同PID、主窗/群窗歧义、实例重建、owner缺失/循环、旧弹窗拒绝、错误锁定；只读探针不得读取Name/Value或执行动作，接口存在不得启用真实启动；新旧真实入口启动阻止、目录锁、配置验证、独立演示真实核心加图取图、源文件保留、关闭释放锁、Tk保存/开始/停止/图库预览。背景模块AST检查禁用输入/Invoke/SetValue调用，不把静态检查当作第三方应用无副作用保证。

恢复：关闭0.4管理器结束演示与探针，保留配置及图库。未修改或迁移原图库、主机微信、系统设置；新数据在USERPROFILE/.tianyi-bot/local-workspace下。旧GUI进程未擅自终止，重开必须重新绑定。0.3源码状态由harness revision保留，但不可用旧路径绕过当前用户禁止前台操作的要求。
# 0.4.2诊断修复审查与恢复

本机标准uiautomation 2.0.29没有IsElementInWindow；此前驱动误用了wxauto扩展。改为有深度/循环限制的同进程父级链归属检查，拒绝同PID其他根窗口。旧用户报告确认未尝试写清，但不能追溯唯一失败原因。

新增15项离线回归覆盖标准接口契约、各前提失败阶段、归属/循环/深度限制、只读与附件保护、草稿读取失败、敏感异常不落盘、只读子进程参数和超时措辞。新微信诊断未由开发代理执行；模拟控件验证不代表微信实际兼容。真实启动保持禁用。

只读诊断使用独立报告，不覆盖用户原始测试结果；恢复时关闭管理器即可，无图库迁移、账号变更或自动重试。本变更不关闭、不归档、不登记最终验收。

# 0.4.1审查和恢复

125/125测试通过，无跳过；新增18项用内存驱动、模拟子进程及真实Tk验证：一次写清、已有草稿含空白/附件符保护、目标和环境变化、写入后异常清理仅自身标记、清理异常保守报告、监测失败锁定、确认门、超时不重试、无敏感结果记录、禁用输入API的AST检查、GUI勾选重置/父绑定锁释放/真实启动继续禁用。修复了操作窗口关闭后残留Tk定时回调的问题。

原生微信试验未执行；仅交付用户主动触发入口。残余风险：UIA调用与窗口变化非原子、10ms采样可能漏瞬态、超时后COM可能仍完成、同名群/同窗换号不能自动鉴别；人工事先核对仍不可省略。状态不确定不自动清空别人的输入或重试，不因测试通过启用机器人。

恢复：关闭管理器；已有图库不迁移不删除，诊断只保存安全报告。若用户主动测试后残留标记，人工核对同学群再删除本次测试草稿；未尝试对真实草稿作自动恢复。旧GUI未强制关闭，本轮不归档、不登记最终验收。
# 0.4.3：标题失败后的受限诊断

用户本轮last-draft-trial.json为header/ManagementError、write_attempted=false、clear_attempted=false；environment_changed=true不能证明机器人抢焦点。当前源码与免费库标题路径基本一致，不擅自猜测新选择器。只读路径在header失败后保存绑定主窗内最多160节点/8层的类型与白名单类名；不读取Name、Value、AutomationId，不保存未知类名或运行时身份；子进程仍15秒超时。不在写入试验中追加此扫描。

新增4项模拟验证：敏感字段不落盘、越界归属拒绝、节点/深度限制、标题失败报告。真实微信结构扫描未由代理执行，实际标题适配和全后台收发仍未完成。关闭管理器即可停止，原图库和报告保留；不归档、不登记验收。
# 0.5空白分身开发自审

新增独立C#源码和Python启动器，使用本机系统编译器成功构建，无额外SDK/安装包下载。模拟/静态验证仅检查打包、启动参数、失败传播、默认无系统变更和安全配置，不能验证RDP运行或输入隔离。

当前残余风险：ActiveX属性兼容、连接失败细节、COM/UI阻塞、窗口隐藏后帧刷新、Windows家庭版实际连接、其他客户端竞争、崩溃后人工恢复。子会话不能锁屏，不是安全沙箱；同用户自启动可能发生。未承诺完全恢复共享服务运行状态，禁止强停其他用户可能使用的服务。测试入口不启动业务程序、不启用真实机器人、不归档。
# 0.5.1失败定位与非连接预检

用户截图显示TargetInvocationException；旧日志没有底层错误。只读复核开关为false、无子会话、TermService运行；完成记录original_service_stopped=false，因此不强停服务。修复外层包装导致定位信息丢失：记录调用名、底层异常类型、HRESULT，不保存异常原文。把控件配置提前到启用之前，修正从未确认创建时的“已退出”误导提示。

真实环境执行新建、透明且不激活的原生控件实例PrepareLocal，成功，未调用Connect/Native启用/注销、未操作既有应用。该结果仅证明非连接初始化配置。新增3项静态回归检查顺序、预检无连接、异常展开。实际连接失败根因仍未确诊，不承诺已修复。恢复日志及图库保留。
# 0.5.2连接配置修复

用户报告确认为Connect阶段ArgumentException/0x80070057。对照BetterGI发现缺失EnableCredSspSupport=true，本次补齐，连接前及Prepare结束后读回localhost、CredSSP、ConnectToChildSession及所有已配置重定向/自动重连禁用项。不放宽隔离、不禁用身份认证、不修改系统安全策略、不自动重试连接。

旧日志保留。原生不连接预检将用于验证实际属性读回，不能替代连接或微信验证；新增两项静态回归防止认证遗漏和连接前核验回退。恢复行为不变，项目保持开发中。
# 0.5.3连续生命周期修复

用户第一轮会话7成功恢复，第二轮会话8归属未知且界面恢复失败。经用户明确授权，已单独核对PID201260、创建时间、程序路径及父会话6，对测试器定向结束并注销分身8，恢复原关闭开关；主会话微信进程保留，恢复记录备份。未修改共享服务。此为故障恢复证据，不是正常退出验收。

代码确认缺陷：结束/COM消息泵期间计时检查可重入；成功退出未清空rdp引用及uncertain。新增共享CycleState控制转换期检查许可，Begin重置旧标记，End完成后清空引用及状态；正常Check也禁止重入。注销以wait=false在后台发起，异步轮询最多25秒，用户确认和整个恢复阶段均阻止正常轮询。历史completed报告不再覆盖删除而转存唯一备份。

编译程序的--cycle-selftest仅运行同一CycleState类，无Native调用，覆盖两轮、转换期重入、旧标记重置、活动期禁止重置；静态测试核对实际Form使用守卫和非阻塞注销。仍缺真实连续两轮/隐藏/后台输入验证，不能宣称已解决所有COM卡顿。变更不归档，故障恢复材料保留。
# 0.5.4 分身心跳验证增量

用户反馈0.5.3两轮均成功，属于用户报告的真实环境结果，未独立观测。新增用户在子会话手动启动的消息循环探针，主机会话绝不运行探针；固定有限报告、原子替换、单实例锁、会话匹配、五秒过期、隐藏基线重置。只验证消息循环，不收集输入、不调用微信、不宣称输入隔离或屏幕渲染通过。无自动提权、登录、凭据保存或系统组件修复。

恢复：关闭探针停止写入；原控制器结束本次分身并恢复开关。保留上一版缓存，可回退0.5.3，禁止通过删除恢复记录强行重启。实际隐藏心跳仍待用户测试。
# 0.6.0 分身微信绑定入口

停止增加独立心跳测试。现有scanner的SM_REMOTESESSION拒绝策略会阻断分身，新增独立受上下文约束的scanner模式，不放宽主机默认策略。控制器记录父会话/PID/创建时间；子入口检查父进程存活、创建时间和会话归属，再只扫描同会话微信。记录是防误操作边界，不是安全认证；人工窗口绑定不证明账号身份。进程重启、恢复记录失效或扫描失败取消绑定。新入口不启动微信，不收发，不修改主机微信。使用已安装包回归，Windows/微信上下文采用模拟，不冒充实机连接。

回退：正常结束分身后使用已有0.5.3控制器；旧图库文件未修改。仍需实现监听收发、兼容性与后台隔离实测。0.5.4用户报告IOException，不能记录心跳通过；不假定其底层原因已经查明。
# 0.6.1 直接启动快捷方式

用户报告火绒删除旧打开分身.lnk，并批准改为直接程序快捷方式。去掉PowerShell命令链，新增标准requireAdministrator/uiAccess=false应用清单；管理员版本缓存摘要包含清单，离线自测仍使用非管理员版本。桌面只指向本地产物并传--open-child，不恢复隔离、不添加安全例外、不代替用户确认UAC。管理员产物仅编译/静态验证，不执行；是否被火绒接受和实际创建分身仍待用户测试。回退可移除新快捷方式，不能恢复旧命令链。图库/凭据/系统策略不变。
# 0.6.2 分身单窗口自动连接

用户明确分身内唯一微信即机器人号，移除分身界面选窗口/前台倒计时/人工账号勾选；保留当前分身范围、单候选、窗口身份重验和主机拒绝。主机旧入口行为不变。控制器身份读取改为PROCESS_QUERY_LIMITED_INFORMATION，避免不必要的跨会话访问权限；异常按阶段报告且不含账号、路径或异常原文。旧实测异常被吞没，不能断言其根因已证实；需用户在当前分身重开GUI验证。不更改现有控制器和安全策略，不自动登录或发送。
# 0.6.3 状态发布取代进程读取

用户截图确认读取控制器身份PermissionError/5。移除分身读取父进程名称/创建时间及所有OpenProcess调用；控制器仅在RDP连接且子会话身份匹配后续写connected记录，断开写disconnected，结束原有ending恢复流程保留。分身验证自身编号、父控制台编号、connected状态及8秒新鲜度。模拟覆盖过期/未来/非有限值/旧版/错会话/断开；不冒充实机验证。共享文件为同用户防误操作机制，不是防恶意篡改的授权通道；目前无真实收发。

回退只在用户正常结束当前会话后操作。安装新包和快捷方式，不注销或终止当前控制器。旧控制器不会发布新时间，必须正常重建一次；不能伪造新记录兼容旧控制器。安全软件和系统权限保持不变。
# 0.6.4 只读群观察

用户截图证明分身微信已连接，属用户实测证据，非代理自主收发验证。新增用户按钮启动的单群UIA只读消息控件观察，不调用wxauto初始化/输入API，不读取消息正文，不接核心回调。参考已安装固定库ChatBox消息列表与标题定位，当前版本结构仍待实际测试。双重群标题/归属/窗口复核，隔离进程8秒超时，首次列表为历史基线，列表重建停止；代次避免停止后在途结果污染重启。历史加载可能误计新增控件，明确不是准确消息计数。

复用现有群配置和图库界面，分身配置页移除旧草稿试验按钮。用户保持当前群；不自动导航，不发送。关闭GUI停止观察，当前控制器/分身不变。回退仅重装上一管理包，不改图库或控制器记录。下一步仍需消息解析与图片收发适配，禁止把mock当真实微信验证。
# 0.6.5 监听结构定位修复

用户截图为群已打开且分身绑定有效，但监听失败。原生库默认searchDepth无限，已排除“仅搜直接子节点”的代码假设；旧代码固定完整AutomationId及Text类型、并双层吞没错误。改为有限结构遍历、标识后缀唯一匹配、消息子树剪枝；保持根归属和群标识检查。阶段/异常类型经白名单跨进程传递，GUI保留；不输出异常原文。模拟变更祖先路径/重复候选/隐私过滤用例不等同真实WeChat4.1结构验收。恢复仅重开GUI，原控制器/微信/图库不动。
# 0.6.6 真实定位证据缺口

用户截图确认locate/ManagementError，不能推断标题、群标识或消息区哪项缺失，也不能假定搜索上限。加入失败原因枚举、候选数量、有限白名单结构自动保存，跨进程再次白名单过滤。只在用户再次启动监听后采集，不由代理跨会话读取聊天UI。不改定位行为，不宣称已解决；回归测试只验证诊断隐私/传递，不是微信兼容性。恢复只需关闭GUI，现有分身/微信和图库保持。遵守当前授权，不发送。
